{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | grep -q '\\.context/.*\\.json$' && echo \"$(date -u +%Y-%m-%dT%H:%M:%SZ) | TASK_TRANSITION | file=$(jq -r '.tool_input.file_path')\" >> .context/audit.log || true"
          }
        ]
      }
    ]
  }
}
