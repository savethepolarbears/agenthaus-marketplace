{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "file_path=$(jq -r '.tool_input.file_path // empty') && if printf '%s\\n' \"$file_path\" | grep -q '\\.context/.*\\.json$'; then printf '%s | TASK_TRANSITION | file=%s\\n' \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\" \"$file_path\" >> .context/audit.log; fi || true"
          }
        ]
      }
    ]
  }
}