---
description: "Serverless Postgres database management via Neon."
globs: "**/*"
alwaysApply: true
---

# data-core

Serverless Postgres database management via Neon.

## MCP Configuration

MCP servers for this plugin are in `.cursor/mcp.json`. Environment variables use `${env:VAR_NAME}` syntax in Cursor.
