# notion-workspace

Interact with your Notion workspace - search pages, query databases and create new content.

## Plugin Reference

@plugins/notion-workspace/README.md

## MCP Setup

Run `agenthaus install --target antigravity` to register the servers from `gemini-settings-snippet.json` in Gemini CLI `settings.json` and Antigravity `mcp_config.json`.
