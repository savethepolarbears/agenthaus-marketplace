{
  "mcpServers": {
    "apple-productivity": {
      "args": [
        "run",
        "--project",
        "${workspaceFolder}/plugins/apple-workflows/mcp",
        "apple-productivity-mcp"
      ],
      "command": "uv",
      "env": {
        "APPLE_REMINDERS_BACKEND": "${env:APPLE_REMINDERS_BACKEND}"
      }
    }
  }
}
