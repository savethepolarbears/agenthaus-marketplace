{
  "_comment": "Add mcpServers entries to your Gemini CLI settings",
  "mcpServers": {
    "textfocus": {
      "args": [
        "-m",
        "textfocus_server"
      ],
      "command": "python",
      "env": {
        "TEXTFOCUS_API_KEY": "${TEXTFOCUS_API_KEY}"
      }
    }
  }
}
