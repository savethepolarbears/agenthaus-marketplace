---
phase: 03-generation-run
plan: 02
subsystem: validation
tags: [cross-platform, validation, agents-md, cursor-mdc, byte-gate]
dependency_graph:
  requires: [03-01]
  provides: []
  affects: [scripts/validate-plugins.sh, plugins/*/.mcp.json]
tech_stack:
  added: []
  patterns: [byte-count gate via wc -c, mdc frontmatter grep check, idempotent generator rerun]
key_files:
  created:
    - plugins/cloudflare-platform/.mcp.json
    - plugins/context7-docs/.mcp.json
    - plugins/github-integration/.mcp.json
    - plugins/neon-db/.mcp.json
    - plugins/notion-workspace/.mcp.json
    - plugins/playwright-testing/.mcp.json
    - plugins/vercel-deploy/.mcp.json
  modified:
    - scripts/validate-plugins.sh
decisions:
  - "validate_agents_md uses wc -c (byte count) not wc -m (char count) — consistent with Buffer.byteLength approach from renderer"
  - "validate_cursor_mdc checks head -10 lines only — frontmatter always appears at top of .mdc files"
  - "Repo-level AGENTS.md check placed after validate_skills_index to logically group all file-level checks together"
  - "7 untracked .mcp.json files (missing from 03-01 commit) staged and committed in this plan"
metrics:
  duration_min: 5
  completed_date: "2026-03-20"
  tasks_completed: 2
  files_changed: 8
---

# Phase 03 Plan 02: Validation Extension and Final Generation Run Summary

Extended `validate-plugins.sh` with AGENTS.md byte-limit enforcement and .cursor/rules/*.mdc frontmatter checks, confirmed generator idempotency, and committed 7 previously untracked .mcp.json files — bringing all 27 plugins to a fully committed, 0-failure validation state.

## What Was Built

**validate_agents_md()** — New function checks that each plugin's `AGENTS.md` file is within the 2 KiB byte budget. Uses `wc -c` for byte-accurate counting, consistent with the generator's `Buffer.byteLength` gate. Called inside the per-plugin loop.

**validate_cursor_mdc()** — New function iterates all `.cursor/rules/*.mdc` files in a plugin and verifies both `description:` and `globs:` fields appear in the first 10 lines of frontmatter. FAIL reported per missing field. Called inside the per-plugin loop.

**Repo-level AGENTS.md check** — Added after `validate_skills_index`, checks that the repo-root `AGENTS.md` stays under 6 KiB (current: 3114 bytes).

**7 .mcp.json files committed** — The standalone plugin `.mcp.json` files for cloudflare-platform, context7-docs, github-integration, neon-db, notion-workspace, playwright-testing, and vercel-deploy were generated in 03-01 but not staged at commit time. Staged and committed here.

## Verification Results

| Check | Result |
|-------|--------|
| `bash -n scripts/validate-plugins.sh` syntax check | PASS |
| `grep -c 'validate_agents_md'` >= 2 | PASS (2) |
| `grep -c 'validate_cursor_mdc'` >= 2 | PASS (2) |
| `grep 'repo_agents'` present | PASS (3 occurrences) |
| Generator rerun — 0 written, 0 errors | PASS |
| 27 AGENTS.md files | PASS |
| 27 GEMINI.md files | PASS |
| 27 .mdc files | PASS |
| 14 gemini-settings-snippet.json files | PASS |
| 14 .cursor/mcp.json files | PASS |
| Repo AGENTS.md bytes = 3114 (< 6144) | PASS |
| skills_index.json all entries have platforms | PASS |
| `bash scripts/validate-plugins.sh` exit 0 | PASS — 27 passed, 0 failed, 0 warnings |

## Commits

- `f52bfb8`: feat(03-02): extend validate-plugins.sh with cross-platform checks
- `024a9af`: feat(03): generate cross-platform configs for all 27 plugins

## Deviations from Plan

**[Rule 2 - Missing Output] Committed 7 untracked .mcp.json files missed in 03-01**
- **Found during:** Task 2 git status check
- **Issue:** 7 plugin-level `.mcp.json` files (cloudflare-platform, context7-docs, github-integration, neon-db, notion-workspace, playwright-testing, vercel-deploy) were generated by the generator in plan 03-01 but not staged/committed at that time
- **Fix:** Staged and committed in the plan's "all generated files" commit
- **Files modified:** 7 files listed above
- **Commit:** 024a9af

## Self-Check: PASSED

- `scripts/validate-plugins.sh` — syntax clean, 3 new functions added
- `validate-plugins.sh` exits 0 — 27 passed, 0 failed, 0 warnings confirmed
- 7 `.mcp.json` files — committed (create mode confirmed in git commit output)
- Commits f52bfb8 and 024a9af — confirmed in git log
