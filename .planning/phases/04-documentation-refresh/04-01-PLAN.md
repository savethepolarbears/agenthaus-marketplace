---
phase: 04-documentation-refresh
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CONTRIBUTING.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
must_haves:
  truths:
    - "A user on Codex CLI can find a concrete shell command to install a plugin"
    - "A user on Gemini CLI can find instructions to configure MCP and copy GEMINI.md"
    - "A user on Cursor can find instructions to copy .cursor/ and set ${env:VAR} syntax"
    - "A user on Windsurf can find instructions to copy AGENTS.md as .windsurfrules"
    - "A user on Claude Desktop can find instructions to merge claude-desktop-snippet.json"
    - "A contributor can find how to run the generator and what files it produces"
    - "CONTRIBUTING.md explains which plugins trigger platform limitation notices and why"
  artifacts:
    - path: "README.md"
      provides: "Per-platform installation section with commands for all 6 platforms"
      contains: "Codex CLI"
    - path: "CONTRIBUTING.md"
      provides: "Generator usage guide and cross-platform development guide"
      contains: "generate-cross-platform.js"
  key_links:
    - from: "README.md"
      to: "per-plugin README"
      via: "platform support table reference"
      pattern: "platform.*support"
    - from: "CONTRIBUTING.md"
      to: "scripts/generate-cross-platform.js"
      via: "documented run command"
      pattern: "node scripts/generate-cross-platform"
---

<objective>
Update README.md with a concrete per-platform installation section covering all 6 supported platforms (Claude Code, Codex CLI, Gemini CLI, Cursor, Windsurf, Claude Desktop). Update CONTRIBUTING.md with generator usage instructions and cross-platform plugin development guidance.

Purpose: Users on non-Claude-Code platforms need a clear path to install and use plugins. Contributors need to know how to regenerate cross-platform configs after modifying a plugin.
Output: Updated README.md and CONTRIBUTING.md at repo root.
</objective>

<execution_context>
@/Users/klkro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/klkro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-documentation-refresh/04-CONTEXT.md
@README.md
@CONTRIBUTING.md

<interfaces>
<!-- Platform-specific file layout produced by Phase 3 generator -->
<!-- Each plugin directory after Phase 3 contains: -->
<!--   AGENTS.md          — for Codex CLI and Windsurf (prose, under 2 KiB) -->
<!--   GEMINI.md          — for Gemini CLI -->
<!--   .cursor/rules/<name>.mdc — for Cursor (MDC frontmatter format) -->
<!--   gemini-settings-snippet.json — for Gemini CLI MCP config (MCP plugins only) -->
<!--   claude-desktop-snippet.json  — for Claude Desktop MCP config (14 MCP plugins) -->
<!--   .cursor/mcp.json   — for Cursor MCP config (MCP plugins only) -->
<!--   .mcp.json          — for .mcp.json (Claude Code native) -->

<!-- Platform installation mechanics per context (from 04-CONTEXT.md): -->
<!-- Claude Code:     /plugin marketplace add https://github.com/savethepolarbears/agenthaus-marketplace -->
<!-- Codex CLI:       copy plugins/<name>/AGENTS.md to project root as AGENTS.md -->
<!-- Gemini CLI:      copy plugins/<name>/GEMINI.md to project root; -->
<!--                  merge plugins/<name>/gemini-settings-snippet.json into ~/.gemini/settings.json -->
<!-- Cursor:          copy plugins/<name>/.cursor/ to project .cursor/; -->
<!--                  merge plugins/<name>/.cursor/mcp.json into project .cursor/mcp.json -->
<!-- Windsurf:        copy plugins/<name>/AGENTS.md to project root as .windsurfrules -->
<!--                  (Windsurf project-scoped MCP path unconfirmed — direct to global config) -->
<!-- Claude Desktop:  merge plugins/<name>/claude-desktop-snippet.json into -->
<!--                  ~/Library/Application Support/Claude/claude_desktop_config.json (macOS) -->
<!--                  or %APPDATA%\Claude\claude_desktop_config.json (Windows) -->

<!-- Hook limitation: circuit-breaker, shadow-mode, agent-handoff, social-media, -->
<!--                  gog-workspace, wp-cli-fleet have hooks that only run on Claude Code -->
<!-- MCP limitation:  Codex CLI does not implement MCP — MCP tools unavailable on Codex -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add per-platform installation section to README.md</name>
  <files>README.md</files>
  <read_first>README.md, plugins/neon-db/AGENTS.md, plugins/neon-db/claude-desktop-snippet.json</read_first>
  <action>
Replace the existing "Quick Start" section in README.md with an expanded "Installation" section that covers all 6 platforms. Keep the existing "Available Plugins", "Architecture", "Configuration", "Web Storefront", "Validation", "Contributing", and "Resources" sections intact.

The new section structure:

## Installation

### Claude Code (recommended)

```bash
# Add the full marketplace
/plugin marketplace add https://github.com/savethepolarbears/agenthaus-marketplace

# Or install a single plugin
/plugin install social-media@AgentHaus
```

Requires Claude Code 1.0.33+. Gives full access: commands, agents, skills, hooks, and MCP servers.

### Codex CLI

Codex CLI supports prose context files (AGENTS.md) but not MCP servers or hooks.

1. Copy the plugin's AGENTS.md to your project root:
   ```bash
   cp plugins/neon-db/AGENTS.md ./AGENTS.md
   ```
2. Codex reads AGENTS.md automatically on session start.

**Note:** MCP-dependent plugins (neon-db, github-integration, cloudflare-platform, etc.) will document their tools in AGENTS.md but cannot execute them — Codex CLI has no MCP runtime. Hook-dependent plugins list their hooks as prose guidance only.

### Gemini CLI

Gemini CLI reads GEMINI.md context files and supports MCP servers via `~/.gemini/settings.json`.

1. Copy the plugin's GEMINI.md to your project root:
   ```bash
   cp plugins/neon-db/GEMINI.md ./GEMINI.md
   ```
2. For MCP plugins, merge the snippet into your Gemini settings:
   ```bash
   # View the snippet
   cat plugins/neon-db/gemini-settings-snippet.json
   # Manually merge mcpServers block into ~/.gemini/settings.json
   ```
3. Set required environment variables (see each plugin's README).

### Cursor

Cursor reads `.cursor/rules/*.mdc` files and supports MCP servers via `.cursor/mcp.json`.

1. Copy the plugin's Cursor rules to your project:
   ```bash
   cp -r plugins/neon-db/.cursor/rules/ ./.cursor/rules/
   ```
2. For MCP plugins, merge the MCP config:
   ```bash
   # View the snippet
   cat plugins/neon-db/.cursor/mcp.json
   # Manually merge mcpServers into your project's .cursor/mcp.json
   ```
3. Cursor uses `${env:VAR}` syntax for environment variable references in MCP configs.

### Windsurf

Windsurf reads `.windsurfrules` context files from the project root.

1. Copy the plugin's AGENTS.md as `.windsurfrules`:
   ```bash
   cp plugins/neon-db/AGENTS.md ./.windsurfrules
   ```
2. For MCP plugins, configure servers in Windsurf's global MCP settings (Windsurf → Preferences → MCP).

**Note:** Hooks are not supported on Windsurf.

### Claude Desktop

Claude Desktop supports MCP servers configured in `claude_desktop_config.json`. Context files are not applicable.

1. Locate your config file:
   - macOS: `~/Library/Application Support/Claude/claude_desktop_config.json`
   - Windows: `%APPDATA%\Claude\claude_desktop_config.json`
2. Merge the plugin's snippet:
   ```bash
   cat plugins/neon-db/claude-desktop-snippet.json
   ```
   Copy the `mcpServers` entries into your existing config's `mcpServers` object.
3. Restart Claude Desktop to load the new MCP servers.

**Note:** Only MCP plugins provide functionality in Claude Desktop. 14 plugins include `claude-desktop-snippet.json`.

---

After the installation section, update the badge line at the top to reference all 6 platforms:

```
[![Platforms](https://img.shields.io/badge/platforms-6-blue.svg)](#installation)
```

Also update the opening description line from "Claude Code" to "Claude Code and other AI coding assistants":

> A comprehensive marketplace of **27 production-ready plugins** for Claude Code and other AI coding assistants.
  </action>
  <verify>
    <automated>grep -c "Codex CLI\|Gemini CLI\|Cursor\|Windsurf\|Claude Desktop" /Users/klkro/Projects/0.Infra/automation/agenthaus-marketplace/README.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Codex CLI" README.md
    - grep -q "Gemini CLI" README.md
    - grep -q "claude-desktop-snippet.json" README.md
    - grep -q "windsurfrules" README.md
    - grep -q "\.cursor/rules" README.md
    - grep -q "plugin marketplace add" README.md
  </acceptance_criteria>
  <done>README.md contains a concrete installation block for each of the 6 platforms with copy-paste shell commands</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add generator usage and cross-platform dev guide to CONTRIBUTING.md</name>
  <files>CONTRIBUTING.md</files>
  <read_first>CONTRIBUTING.md, scripts/generate-cross-platform.js</read_first>
  <action>
Add two new sections to CONTRIBUTING.md before the existing "Submitting Your Plugin" section:

---

## Cross-Platform Support

AgentHaus plugins target 6 platforms: Claude Code, Codex CLI, Gemini CLI, Cursor, Windsurf, and Claude Desktop. The generator at `scripts/generate-cross-platform.js` reads Claude-native plugin sources and emits per-platform config files automatically.

### Generated Files

For each plugin, the generator produces:

| File | Platform | Purpose |
|------|----------|---------|
| `AGENTS.md` | Codex CLI, Windsurf | Prose context: capabilities, limitations, usage |
| `GEMINI.md` | Gemini CLI | Prose context with MCP configuration guidance |
| `.cursor/rules/<name>.mdc` | Cursor | MDC rule file with description, globs, alwaysApply frontmatter |
| `gemini-settings-snippet.json` | Gemini CLI | mcpServers snippet for `~/.gemini/settings.json` (MCP plugins only) |
| `.cursor/mcp.json` | Cursor | mcpServers config with `${env:VAR}` syntax (MCP plugins only) |
| `claude-desktop-snippet.json` | Claude Desktop | mcpServers snippet for `claude_desktop_config.json` (MCP plugins only) |

The generator is idempotent: re-running on unchanged sources produces byte-for-byte identical output.

### Running the Generator

After editing any plugin source file (plugin.json, SKILL.md, hooks.json, .mcp.json), regenerate all cross-platform configs:

```bash
node scripts/generate-cross-platform.js
```

Then validate the output:

```bash
bash scripts/validate-plugins.sh
```

The validation script checks AGENTS.md byte count (must be under 2 KiB per plugin), .mdc frontmatter validity, and file reference resolution.

### Platform Limitations

The generator embeds platform limitation notices automatically based on plugin capabilities:

- **Hook-dependent plugins** (`circuit-breaker`, `shadow-mode`, `agent-handoff`, `social-media`, `gog-workspace`, `wp-cli-fleet`): Generated files include a notice that hooks are Claude Code-exclusive.
- **MCP-dependent plugins** on Codex CLI: Generated AGENTS.md includes a notice that MCP tools are unavailable on Codex CLI.

If your new plugin uses hooks or MCP servers, the generator detects this from `plugin.json` and adds the correct notices — you do not need to write them manually.

### AGENTS.md Size Budget

Generated `AGENTS.md` files must stay under 2 KiB (2048 bytes). The generator enforces this by truncating content if the budget is exceeded. Keep plugin descriptions and agent prompts concise to stay within budget.

---

Also update the "Available models" line in the Agents section to remove the deprecated pinned model reference:

Change:
```
Available models: `sonnet`, `haiku`, `opus`, `claude-3-7-sonnet-20250219`.
```
To:
```
Available models: `sonnet`, `haiku`, `opus`. Always use the alias, never a pinned version string — pinned versions go stale and break the generator's model validation.
```
  </action>
  <verify>
    <automated>grep -c "generate-cross-platform\|AGENTS.md\|Platform Limitations" /Users/klkro/Projects/0.Infra/automation/agenthaus-marketplace/CONTRIBUTING.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "generate-cross-platform.js" CONTRIBUTING.md
    - grep -q "idempotent" CONTRIBUTING.md
    - grep -q "2 KiB" CONTRIBUTING.md
    - grep -q "Hook-dependent" CONTRIBUTING.md
    - grep -q "claude-desktop-snippet.json" CONTRIBUTING.md
    - grep -v "claude-3-7-sonnet-20250219" CONTRIBUTING.md (deprecated model removed)
  </acceptance_criteria>
  <done>CONTRIBUTING.md has a "Cross-Platform Support" section documenting generator usage, generated file table, run command, and limitation notice logic</done>
</task>

</tasks>

<verification>
- README.md contains installation blocks for all 6 platforms with concrete shell commands
- README.md references claude-desktop-snippet.json by filename
- README.md references .windsurfrules for Windsurf
- CONTRIBUTING.md contains "Cross-Platform Support" section
- CONTRIBUTING.md documents `node scripts/generate-cross-platform.js` run command
- CONTRIBUTING.md lists all 6 generated file types in a table
- CONTRIBUTING.md names the hook-dependent and MCP-limited plugins
- Deprecated claude-3-7-sonnet-20250219 model alias removed from CONTRIBUTING.md agents section
</verification>

<success_criteria>
1. A user on any of the 6 supported platforms can find concrete installation steps in README.md
2. A contributor knows to run `node scripts/generate-cross-platform.js` after editing plugin sources
3. CONTRIBUTING.md explains what files the generator produces and why limitations appear
</success_criteria>

<output>
After completion, create `.planning/phases/04-documentation-refresh/04-01-SUMMARY.md`
</output>
